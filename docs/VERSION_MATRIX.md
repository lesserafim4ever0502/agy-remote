# Compatibility matrix

This file is intended to be updated after real-machine tests.

| Antigravity build | Discovery | Agent stream | Terminal v2 | Browser | Notes |
|---|---|---|---|---|---|
| extracted schema build 930583198 / v2.1.1 | schema-known | schema-known | schema-known | schema-known | Not locally validated in this repo build environment |
| user's installed build | TODO | TODO | TODO | TODO | Run `npm run doctor` |

"schema-known" means the method/message exists in the public extracted schema used during implementation. It does not mean Google guarantees compatibility.
